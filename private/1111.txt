---
title: 给刘老师的生日秘密信件
date: 2025-11-27 00:00:00
categories: private
tags:
toc: false
comments: false
permalink: /private/yee_yi_birthday/
---

{% raw %}
<!-- ====================  1.  样式  ==================== -->
<style>
/* 清除核心容器#page的白色背景和边框 */
#page {
    background: transparent !important; /* 完全透明背景 */
    border: none !important; /* 去掉边框 */
    padding: 0 !important; /* 清除内边距（解决留白） */
    margin: 0 !important; /* 清除外边距 */
    box-shadow: none !important; /* 去掉可能的阴影 */
}

/* 确保其父容器也无背景（保险措施） */
#content-inner, .layout {
    background: transparent !important;
    padding: 0 !important;
    margin: 0 !important;
}

/* ① 主题元素隐藏 */
#page-header,#nav,#sidebar,#footer,.post-copyright,.post-meta,.post-tags,
.breadcrumbs,.aside-content,.sticky_layout,#rightside,.post-title,.layout>.aside,
.content-wrap>.aside,.sidebar-dock,.toc-container{ display:none !important; pointer-events:none !important;}

/* ② 全屏背景：合照 + 黑色半透明遮罩 */
html,body{
    margin:0 !important; padding:0 !important; height:100vh !important;
    display:flex !important; align-items:center !important; justify-content:center !important;
    overflow-x:hidden !important;
}
body{
    height:auto !important; min-height:100vh !important;
    background:#000;                      /* 底色 */
}
body::before{                            /* 背景图 */
    content:"";
    position:fixed;
    inset:0; /* 全屏覆盖 */
    background:url("/images/private_pic/2025-11-26-yee-yi-birthday_2.jpg") center/cover no-repeat;
    filter:brightness(0.7) blur(1.5px); /* 亮度+模糊 组合效果 */
    z-index:-2;
}

body::after{                             /* 黑色半透明遮罩 */
    content:"";
    position:fixed;
    inset:0;
    background:rgba(0,0,0,.35);
    z-index:-1;
}

/* ③ 信件容器 */
#birthday-letter-container{
    width:800px !important;              /* 电脑端宽度 */
    margin:50px auto !important;
    font-family:'Segoe UI','Microsoft YaHei',sans-serif !important;
    background:none;                     /* 无背景 */
    backdrop-filter:none;                /* 无模糊 */
    border: none;                        /* 无边框 */
    border-radius:16px !important;       /* 圆角 */
    position:relative !important;
    z-index:999 !important;
}

/* 标题样式：带模糊阴影 */
.birthday-title{
    font-size:3em;
    color:#fff;
    text-align:center;
    text-shadow: 0 2px 15px rgba(0,0,0,0.6), /* 外层阴影 */
                 0 1px 5px rgba(255,255,255,0.2); /* 内层高光 */
    animation:bounce 2s infinite;
}

/* 打字机信件容器（合并重复样式，保留完整定义） */
#letter{
    font-size:1.25em;
    line-height:1.9;
    margin:30px 0;
    padding:25px;
    background:none;          /* 无背景 */
    border-left:3px solid rgba(255,255,255,.6); /* 左侧装饰线 */
    border-radius:10px;
    min-height:280px;
    color:#fff;               /* 白色文字 */
    text-shadow: 0 1px 8px rgba(0,0,0,0.7); /* 文字模糊阴影 */
    animation:slideDown .8s ease-out; /* 下滑动画 */
}

/* 闪烁光标 */
#cursor{
    display:inline-block; width:2px; height:1.1em;
    background:#ff6b6b; vertical-align:text-bottom;
    animation:blink 1s infinite;
}

/* 按钮行 */
.button-row{ text-align:center; margin-top:20px; }
.birthday-button{
    background:linear-gradient(45deg,#ff6b6b,#ff8787);
    color:#fff; border:none; padding:12px 26px; font-size:1.1em;
    border-radius:50px; cursor:pointer; margin:8px;
    box-shadow:0 4px 15px rgba(255,107,107,.4);
    transition:all .3s ease;
}
.birthday-button:hover{ transform:translateY(-3px); box-shadow:0 6px 20px rgba(255,107,107,.6); }

/* 弹窗样式 */
.modal{ display:none; position:fixed; z-index:1000; inset:0; background:rgba(0,0,0,.8); }
.modal-content{ background:#fff9e6; margin:15% auto; padding:30px; border-radius:20px; width:80%; max-width:500px; text-align:center; animation:modalFadeIn .5s; }
.close{ float:right; font-size:28px; cursor:pointer; color:#aaa; }
.close:hover{ color:#e91e63; }
.cake-icon{ font-size:4em; animation:none; }

/* 爱心飘落 */
.heart{ position:fixed; top:-50px; left:calc(100vw * var(--rand-x)); font-size:20px; color:#ff6b6b; pointer-events:none; animation:float 6s linear infinite; }
@keyframes float{ 0%{transform:translateY(0) rotate(0deg); opacity:1;} 100%{transform:translateY(110vh) rotate(360deg); opacity:0;} }
.heart{ z-index:9999; }

/* 关键帧动画 */
@keyframes modalFadeIn{ from{opacity:0; transform:scale(.8);} to{opacity:1; transform:scale(1);} }
@keyframes bounce{ 0%,20%,50%,80%,100%{transform:translateY(0);} 40%{transform:translateY(-20px);} 60%{transform:translateY(-10px);} }
@keyframes slideDown { 0%{transform:translateY(-50px); opacity:0;} 100%{transform:translateY(0); opacity:1;} }
@keyframes blink { 0%,100%{opacity:1;} 50%{opacity:0;} }

/* 手机适配：媒体查询（放在<style>内，确保CSS生效） */
@media (max-width: 768px) {
    #birthday-letter-container {
        width: 90% !important; /* 手机端宽度 */
        max-width: 300px !important;
        padding: 20px 10px !important;
    }
    .birthday-title {
        font-size: 2em !important; /* 标题缩小 */
    }
    #letter {
        font-size: 1em !important; /* 正文缩小 */
        padding: 15px !important;
        min-height: auto !important;
    }
    .birthday-button {
        padding: 10px 20px !important;
        font-size: 1em !important;
        margin: 5px 3px !important;
    }
    .button-row {
        margin-top: 15px !important;
    }
    body::before {
        background-size: cover !important; /* 适配手机背景 */
    }
}
</style>

<!-- ====================  2.  结构  ==================== -->
<div id="birthday-letter-container">
    <h1 class="birthday-title">🎉 生日快乐！🎂</h1>

    <!-- 打字机信件 -->
    <div id="letter">
        <span id="typing"></span><span id="cursor"></span>
    </div>

    <div class="button-row">
        <button class="birthday-button" onclick="openModal()">🎁 惊喜祝福</button>
        <button class="birthday-button" onclick="playMusic()">🎵 生日歌</button>
        <button class="birthday-button" onclick="playYiYangQianXi()">🎶 易烊千玺</button>
    </div>
</div>

<!-- 弹窗 -->
<div id="birthdayModal" class="modal">
    <div class="modal-content">
        <span class="close" onclick="closeModal()">&times;</span>
        <div class="cake-icon">🎂</div>
        <h2 style="color:#e91e63;">🌟 惊喜祝福！</h2>
        <p style="font-size:1.2em; color:#333;">
            愿你像这颗星星一样闪闪发光，<br>
            愿你的生活像这蛋糕一样甜蜜，<br>
            愿你的每一天都比昨天更幸福！
        </p>
        <div style="font-size:2em; margin:20px 0;">🌈 ✨ 🎊 🎈 🌟</div>
    </div>
</div>

<!-- ====================  3.  脚本  ==================== -->
<script>
/* ---------- 打字机 ---------- */
const letterText =
`
亲爱的刘老师🥰，21岁限定快乐加载完毕！🎂🎉🥳

21岁的人生新篇章正式解锁啦～
愿你比以往更耀眼、更鲜活，像星星一样自带光芒✨🌟！

新的一岁，必须被好运和美好环绕呀！💫
祝绩点up up📈、身边爱意爆棚💖、快乐值满格🥳、前路一路开挂🚀
——所有愿望都能如期实现！

以后的每一年、每一个新阶段，我都会稳稳站在你身边呀～💪
支持刘老师的所有热爱和选择，永远做你的头号粉丝+最强后盾！🌟

刘老师能不能在今天晚上就光速读完博士🎓
明早起来就任职博导👑，我在早八前就收拾好来投奔您～

和Yee的生日就差一天的神仙缘分谁懂啊！🍀
咱们刘老师果然是天选幸运儿，自带锦鲤体质，连生日都这么有仪式感✨

🎁 悄悄说，还有两份「专属定制惊喜」正在快马加鞭赶来的路上呀～
因为想给你独一无二的仪式感，提前一周多就下单啦！⏳ 再等等就可以解锁双倍快乐啦～

寿星今天要早点休息呀～🌙💤 养足精神，明天继续做闪闪发光的21岁少女！😉
晚安安，我的宝藏刘老师～😘💖 新岁快乐，永远明媚！

（Ai味道浓浓的是因为——
如果不是手写的我不是写得太寡淡就是太煽情，我真的尝试过了......

                                    From 你的饼饼！
                                    2025.11.17
                                    00:00:00
`;

const typingEl = document.getElementById('typing');
let idx = 0;

function typeChar() {
    if (idx >= letterText.length) return;
    const ch = letterText.charAt(idx);
    if (ch === '\n') {
        typingEl.innerHTML += '<br>';
    } else {
        typingEl.innerHTML += ch;
    }
    idx++;
    setTimeout(typeChar, 60);
}

window.addEventListener('DOMContentLoaded', typeChar); /* 启动打字 */

/* -------------- 爱心飘落 -------------- */
function createHeart() {
    const h = document.createElement('div');
    h.className = 'heart';
    h.innerHTML = '❤️';
    h.style.setProperty('--rand-x', Math.random());
    document.body.appendChild(h);
    setTimeout(() => h.remove(), 6000);
}
setInterval(createHeart, 300);

/* -------------- 弹窗控制 -------------- */
function openModal() { document.getElementById('birthdayModal').style.display = 'block'; }
function closeModal() { document.getElementById('birthdayModal').style.display = 'none'; }
window.onclick = function (e) {
    const m = document.getElementById('birthdayModal');
    if (e.target === m) m.style.display = 'none';
};

/* -------------- 音乐播放 -------------- */
// 生日歌
function playMusic() {
    const ctx = new (window.AudioContext || window.webkitAudioContext)();
    const notes = [
        264,264,297,264,352,330,0, // 第6个音符后停顿（对应“祝你生日快乐”第一句结尾）
        264,264,297,264,396,352,0, // 第二句结尾停顿
        264,264,528,440,352,330,297,0, // 第三句结尾停顿
        466,466,440,352,396,352       // 最后一句无停顿
    ];
    const dur = 0.6;
    notes.forEach((f, i) => {
        setTimeout(() => {
            const osc = ctx.createOscillator();
            const gain = ctx.createGain();
            osc.connect(gain); gain.connect(ctx.destination);
            osc.frequency.value = f; osc.type = 'sine';
            gain.gain.setValueAtTime(0.3, ctx.currentTime);
            gain.gain.exponentialRampToValueAtTime(0.01, ctx.currentTime + dur);
            osc.start(); osc.stop(ctx.currentTime + dur);
        }, i * dur * 1000);
    });
}

// 易烊千玺歌曲
let yiYangQianXiAudio = null;
function playYiYangQianXi() {
    if (!yiYangQianXiAudio) {
        yiYangQianXiAudio = new Audio("/images/private_pic/易烊千玺 - 粉雾海.mp3");
    }
    yiYangQianXiAudio.currentTime = 0;
    yiYangQianXiAudio.play().catch(err => alert("播放失败，请检查链接或允许浏览器播放音频~"));
}

/* -------------- 加载欢迎 -------------- */
window.addEventListener('load', () => {
    setTimeout(() => alert('🎉 欢迎来到生日祝福页面！'), 1000);
});
</script>
{% endraw %}